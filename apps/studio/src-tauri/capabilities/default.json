{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main studio window.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
