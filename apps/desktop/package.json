{
  "name": "desktop",
  "productName": "desktop",
  "version": "0.1.4",
  "description": "Vailabel Studio Desktop",
  "main": "dist/main.js",
  "scripts": {
    "copy-python": "copyfiles -u -1 ./src/ai/requirements.txt ./dist/ai && copyfiles -u -1 ./src/ai/yolo.py ./dist/ai",
    "build": "tsc &&  electron-builder",
    "copy-migration": "copyfiles -u 3 src/db/migrations/**/* dist/db/migrations",
    "pre-dev": "yarn copy-migration && yarn copy-python",
    "build:publish": "yarn prebuild && tsc && electron-builder --publish always",
    "dev": "tsc && yarn pre-dev && electron . --trace-warnings"
  },
  "keywords": [],
  "author": {
    "name": "Vichea",
    "email": "nathvichea1@gmail.com",
    "url": "https://github.com/vicheanath"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/semver": "^7.7.0",
    "@vailabel/core": "*",
    "electron": "36.2.1",
    "electron-builder": "^26.0.12"
  },
  "dependencies": {
    "electron-updater": "^6.6.2",
    "sqlite3": "^5.1.7"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vailabel/vailabel-studio.git"
  }
}
